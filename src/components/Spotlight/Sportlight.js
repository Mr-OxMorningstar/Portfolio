import React from "react";
import './style.scss';

const SportLight =({ClassName})=>{

    return (
        <div className={`homelight ${ClassName}`} style={{ display: 'block' }}></div>
    )
}
export default SportLight